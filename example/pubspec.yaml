name: example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  open_dropdown:
    git: https://github.com/abbas-25/open_dropdown
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true